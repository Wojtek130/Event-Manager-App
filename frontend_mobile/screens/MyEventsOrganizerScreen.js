import { Text, View } from "react-native";

const MyEventsOrganizerScreen = function ({ navigation }) {
    return (
      <View
        style={{
          flex: 1,
          alignItems: "center",
          justifyContent: "center",
          backgroundColor: "rgb(80,70,50)",
        }}
      >
        <Text>MyEventsOrganizer</Text>
      </View>
    );
  };

export default MyEventsOrganizerScreen;
